<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- CAMERA PREVIEW -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- ROI (ȚINTA) -->
    <com.example.feederbitealarm.RoiOverlayView
        android:id="@+id/roiOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- TIP OVERLAY (INSULĂ, RAMĂ, PUNCTE, CENTROID, FLASH) -->
    <com.example.feederbitealarm.TipOverlayView
        android:id="@+id/tipOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- DEBUG OVERLAY -->
    <com.example.feederbitealarm.DebugOverlayView
        android:id="@+id/debugOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <!-- BUTOANE SENSIBILITATE PE DREAPTA, MICI, CU FUNDAL SEMITRANSPARENT -->
    <LinearLayout
        android:id="@+id/sensitivityButtons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:orientation="vertical"
        android:padding="8dp"
        android:layout_marginEnd="12dp"
        android:background="#66000000">

        <Button
            android:id="@+id/btnS1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="S1"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

        <Button
            android:id="@+id/btnS2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="S2"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

        <Button
            android:id="@+id/btnS3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="S3"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

        <Button
            android:id="@+id/btnS4"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="S4"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />
    </LinearLayout>

    <!-- BUTOANE ZOOM -->
    <LinearLayout
        android:id="@+id/zoomButtons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:orientation="horizontal"
        android:padding="12dp">

        <Button
            android:id="@+id/btnZoom1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1×" />

        <Button
            android:id="@+id/btnZoom3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="3×" />

        <Button
            android:id="@+id/btnZoom6"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="6×" />

        <Button
            android:id="@+id/btnZoom10"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="8×" />
    </LinearLayout>

    <!-- BUTON SET TIP -->
    <Button
        android:id="@+id/btnSetTip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SET TIP"
        android:layout_gravity="top|end"
        android:layout_margin="20dp" />

</FrameLayout>
